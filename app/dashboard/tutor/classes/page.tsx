import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
import { listAssignedClasses } from '../classes-data'
import Link from 'next/link'
export default async function TutorClassesPage(){const classes=await listAssignedClasses(); return <main className="container mx-auto space-y-6 p-6"><Card><CardHeader><CardTitle>My classes</CardTitle><CardDescription>Assigned classes only.</CardDescription></CardHeader><CardContent className="grid gap-4 md:grid-cols-2">{classes.length===0?<p className="text-muted-foreground">No assigned classes yet.</p>:classes.map(c=><Link key={c.id} href={`/dashboard/tutor/classes/${c.id}`} className="rounded-lg border p-4 hover:bg-muted/50"><h3 className="font-semibold">{c.subjectName||'Class'} - {c.studentName}</h3><p className="text-sm text-muted-foreground">{c.scheduleNotes||'No schedule notes'}</p><p className="text-sm">{c.status} / {c.mode}</p></Link>)}</CardContent></Card></main>}
